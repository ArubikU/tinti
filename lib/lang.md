# Language system for the page
# in public folder create a folder called "lang"
# inside save index.json en.json es.json etc.
# index is who manages what languages are available and their display names in every other language
# the regional to detect the default language to the user
# You will need to create a translation lib to use this system
# replace all text in spanish the page with the translation lib
# and create the es.json file with the translations
# to set the language use the user icon in the top right corner
# to display a custom dropdown with settings like "change language"/ "change display name"/ "change avatar url"
# "change language" is the unique of these 3 will still show when the user is not logged in
# saved on local storage